# v1.4.1 Emergency Patch Checklist

## Completed
- [x] Replace "Rubles" with "in-game USD" across listing currency text (create, listing detail, admin prices)
- [x] **LFG posts not submitting** — `LFG_POST_DURATION_HOURS` undefined in service, causing silent crash on submit
- [x] Stuck spinner fixes (8 screens) — `try/finally` added so loading state always resets on error:
  - create listing
  - edit listing
  - delete listing
  - toggle listing active
  - mark listing sold
  - send message
  - confirm trade complete
  - submit support ticket
  - submit trade rating
- [x] Edit listing missing `wantInReturn` validation — blank field now blocked same as create
- [x] Long press to delete own messages in conversation (optimistic UI)
- [x] `supabase/message_delete.sql` — RLS policy for user message deletes

## Before Pushing
- [ ] Run `supabase/message_delete.sql` in Supabase SQL Editor

## Notes
All changes are committed on `master`. Push when SQL is confirmed live.
